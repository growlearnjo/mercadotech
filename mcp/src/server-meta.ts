/**
 * Identidad del servidor, en su propio archivo: `server.ts` registra los
 * resources y los resources necesitan estos valores para el folleto `info`.
 * Compartirlos desde aquí evita el import circular.
 */
export const SERVER_NAME = "mercadotech";
export const SERVER_VERSION = "0.1.0";
